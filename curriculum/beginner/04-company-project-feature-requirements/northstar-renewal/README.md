# Northstar renewal governance workshop

Ticket `AI-2048` asks the Underwriter Assistant to recommend policy-renewal
actions and suggests that high-confidence recommendations might eventually skip
manual review. Do not start by coding that suggestion.

Work through the sources in this order:

1. read the ticket and evidence-backed change context;
2. inspect enterprise, platform, and underwriting requirements under `catalog/`;
3. inspect the source-oriented project manifest, architecture specialization,
   local agent instructions, and execution boundary;
4. complete every artifact under `workshop/starter/`;
5. run `../lab.py` and inject the documented failures; and
6. compare your decisions with `reference/`, including the release snapshot
   and bounded runtime-control evidence.

The reference package covers the complete practical path: locate requirements,
map owners, connect relationships, reject the ticket's review-weakening
hypothesis, specialize the review mechanism, derive feature behavior, map
enforcement, generate an agent boundary, request scope expansion for a hidden
identity dependency, propagate `AI-030` v4, snapshot the release context, and
evaluate one runtime evidence window.

All identities, approvals, revisions, services, and controls are fictional
training data. A structurally valid record is not authenticated policy,
authorization, deployed enforcement, or legal interpretation.
