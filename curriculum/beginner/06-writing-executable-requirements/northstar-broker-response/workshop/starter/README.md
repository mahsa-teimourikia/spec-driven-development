# AI-2219 starter workspace

Work from the ticket and sources, not the reference package.

Complete the glossary, requirements, scenarios, decision table, state machine, and `ProposedUpdate`
contract. Preserve stable IDs and add explicit relationships. Your result should make these failures
observable: unauthorized response, verified-value conflict, stale proposal, duplicate response,
unknown field, ambiguous mapping, invalid value, source conflict, and attachment-only evidence.

Do not select a model or framework, approve an unresolved policy, or let a model-supplied status
authorize mutation.
