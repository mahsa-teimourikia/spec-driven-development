[project]
name = "spec-driven-development-course"
version = "0.1.0"
description = "Credential-free labs for the Enterprise Spec-Driven Development course"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Mahsa Teimourikia" }]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
